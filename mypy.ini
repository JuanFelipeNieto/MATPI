[mypy]
# Ignore missing type stubs for third-party libraries globally.
# This prevents Mypy from complaining about packages without type definitions (like xhtml2pdf, matplotlib, etc.).
ignore_missing_imports = True
